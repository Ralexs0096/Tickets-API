{
  "type": "object",
  "title": "Error",
  "description": "An unknown error occurred while attempting to retrieve the data.",
  "required": ["error"],
  "properties": {
    "error": {
      "$ref": "ErrorSchemaJson"
    }
  }
}
