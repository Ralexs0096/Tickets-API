{
  "title": "InternalError",
  "description": "An unknown error occurred while attempting to retrieve the data.",
  "type": "object",
  "required": ["error"],
  "properties": {
    "error": {
      "type": "object",
      "required": ["code", "message"],
      "properties": {
        "code": {
          "type": "string",
          "description": "Code of error."
        },
        "error": {
          "type": "string",
          "description": "Error that has happened."
        },
        "message": {
          "type": "string",
          "description": "Message with information about the error."
        }
      }
    }
  }
}
