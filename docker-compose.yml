version: '3.9'

services:
  sqlserver:
    image: mcr.microsoft.com/azure-sql-edge:latest
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Pa33word!
    ports:
      - "1433:1433"
    volumes:
      - ./sqlserver-data:/var/opt/mssql
